2.string methods (contact, split, ...)
answer:
string concat:it will join the two strings with each other 
string length:it will tells the length of the string.
toUpperCase();:
it will convert the string into uppercase
toLowerCase();:
it will convert the string into lowercase

split:it will split the string .
chatAt: it will gives the position of the character in that particular string.
charCodeAt: it will gives the position of the charater in entire document.
replace:it will replace the code with use choice code.can replace the string by using .replace method.we can replace the string globally too.by using.

(/movie/g,leela);//tring movie will globally replace with leela string
search:it will search the string and returns the position of the string.
slice():it will extract the portion of the tring.can give negitive values in the slice ,it will take from the last to first in reverse order.
slice(6,16):will give from 6 to 15 characters.
slice(-16,-6):return the string in reverse order.
slice(6):return entire string starts from character 6.
slice(-16):return entire string starts from character 16 in reverse order.

substring():slice and substring are same but substring can not accept the negitive values.
substr():it will extract the portion of the string.
substr(7,13):return the string which starts from 7 and will take upto 13 character.
indexof():it will give the position of the string from left to right.
lastIndexof():it will give the position of the string from right to left that is las to first.



references:
1.https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String
2.https://www.sitepoint.com/15-javascript-string-functions/
3.https://msdn.microsoft.com/en-us/library/ecczf11c(v=vs.94).aspx
4.https://www.impressivewebs.com/javascript-string-methods-reference/
5.http://www.quirksmode.org/js/strings.html
